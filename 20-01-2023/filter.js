import { productList, cardGenerator, headerGenerator } from "./script.js";

const filter1 = document.querySelector("#selectBtn1_100");
const filter2 = document.querySelector("#selectBtn101_");

filter1.addEventListener("click", () => {
  //   const filteredProducts = productList.filter((item) => item.price < 101);

  //   cardGenerator(filteredProducts);
  console.log("ciao");
});

// filter2.addEventListener("click", () => {
//   const filteredProducts = productList.filter((item) => item.price > 100);

//   cardGenerator(filteredProducts);
// });
